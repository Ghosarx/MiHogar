spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mihogar?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Lima&characterEncoding=UTF-8
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  sql:
    init:
      mode: always
      data-locations: classpath:data.sql

  mail:
    host: smtp.mailtrap.io
    port: 587
    username: TU_MAILTRAP_USER       # Reemplaza con tu usuario de Mailtrap
    password: TU_MAILTRAP_PASSWORD   # Reemplaza con tu contraseña de Mailtrap
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 50MB

server:
  port: 8080

app:
  cloudinary:
    cloud-name: TU_CLOUD_NAME      # Reemplaza con tu Cloudinary cloud name
    api-key: TU_API_KEY            # Reemplaza con tu Cloudinary API key
    api-secret: TU_API_SECRET      # Reemplaza con tu Cloudinary API secret
  jwt:
    secret: mihogar-super-secret-key-development-2024-change-in-production
    expiration: 3600000          # 1 hora en ms
    refresh-expiration: 604800000 # 7 días en ms
  cors:
    allowed-origins: http://localhost:4200
  upload:
    dir: uploads/properties

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
